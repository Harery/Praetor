# Phase 0 — Source Resolution

**Wall-clock**: 5 minutes. **Owner**: Coordinator (no agent spawned).

Resolution priority: GitHub → local path → uploads → in-context → HALT.

Records: SOURCE_TYPE, SOURCE_ROOT, commit/snapshot.

## v2 Note

